// Layout dimensions and breakpoints

// Desktop navigation is now a full-width header, so content no longer needs a left offset.
export const SIDEBAR_WIDTH = 0;
export const SECOND_BRAIN_SIDEBAR_WIDTH = 260;
export const MOBILE_NAV_HEIGHT = 57; // MobileNav: py-3 (24px) + logo h-8 (32px) + 1px border
