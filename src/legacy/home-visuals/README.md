# Home visual engine

The application exposes one clock-grid landing page at `/home`. The numbered
home experiments and their routes have been removed. Variant 2 remains an
internal vector-field renderer used only by `/about`.
