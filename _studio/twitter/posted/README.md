# Posted

What went out, moved here from `../queue/` with `status: posted`, the `posted:` link and the date in the name (`<date>-<slug>.md`), image still beside it. The history of the account and the evidence for `../STRATEGY.md`: when a piece gets signal from outside the starting graph (likes, replies, quotes or follows from strangers), add a line under the rule saying what happened, so the format can be recognised and repeated on purpose.
